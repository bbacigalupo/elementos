node_modules/
dist/
*.tgz
.DS_Store

# Claves locales (LOCATIONIQ_KEY). Nunca se versionan; ver README.
.env
.env.local
!.env.example

# Archivos de prueba servidos por el playground.
demo/public/

# Vercel CLI (link local del proyecto, no algo que se versione).
.vercel

# Bundle de wp-embed copiado al plugin, y el .zip empaquetado para subir a
# WordPress: son build output regenerable (npm run build -w @bbacigalupo/wp-embed
# + wp-plugin/README.md), mismo criterio que dist/ en el resto del repo.
wp-plugin/*.zip
wp-plugin/allride-geocodificador/assets/*.js
wp-plugin/allride-geocodificador/assets/*.css
